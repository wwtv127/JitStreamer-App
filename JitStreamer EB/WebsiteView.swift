import UIKit
import SwiftUI
import WebKit

struct WebsiteView: View {
    var body: some View {
        VStack {
            WebView()
                .background(Color.black)
                .ignoresSafeArea()
            
        }
    }
}

